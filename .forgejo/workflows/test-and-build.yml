name: test-and-build

on:
  push: {}

permissions:
  contents: write
  issues: write

jobs:
  test-and-build:
    defaults:
      run:
        shell: bash

    container:
      image: ghcr.io/luzifer-docker/action-env:master@sha256:a511c8f8c84804ec9d9641daa9f994858e4a80a57744ce88c669227a84f1d500
      env:
        CGO_ENABLED: 0
        GOPATH: /go

    runs-on: forgejo

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Marking workdir safe
        run: git config --global --add safe.directory /__w/korvike/korvike

      - name: 'Lint and test code'
        run: |
          make test trivy

      - name: Do a quick local compile
        run: |
          go build .
